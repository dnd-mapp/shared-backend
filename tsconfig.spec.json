{
    "extends": "./tsconfig.json",
    "include": ["test/**/*.ts", "src/**/*.spec.ts"],
    "exclude": ["node_modules", "dist"],
    "compilerOptions": {
        "types": ["node", "vitest/globals"]
    }
}
